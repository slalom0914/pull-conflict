## 충돌사례

### CASE#1
원격레포와 로컬 레포의 커밋 이력이 같은 상황에서 로컬에서
working directory에서의 수정 이후 git pull한다면?

```bash
# pull = fetch + merge
# fetch는 성공인데 merge가 실패한 경우
git pull origin main
```
충돌이 아니므로 그냥 git push하면 된다.

### CASE#2
